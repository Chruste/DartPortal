CREATE TABLE IF NOT EXISTS shanghai21_sessions (
    id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
    owner_user_id INT UNSIGNED NOT NULL,
    participant_summary VARCHAR(1000) NOT NULL DEFAULT '',
    participants_json JSON NOT NULL,
    state_json JSON NOT NULL,
    participant_count SMALLINT UNSIGNED NOT NULL DEFAULT 1,
    event_count INT UNSIGNED NOT NULL DEFAULT 0,
    last_event_at DATETIME(3) NULL DEFAULT NULL,
    is_deleted TINYINT(1) NOT NULL DEFAULT 0,
    created_at DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    updated_at DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
    deleted_at DATETIME(3) NULL DEFAULT NULL,
    CONSTRAINT fk_shanghai21_sessions_owner FOREIGN KEY (owner_user_id) REFERENCES portal_users (id),
    INDEX idx_shanghai21_sessions_owner_active_updated (owner_user_id, is_deleted, updated_at),
    INDEX idx_shanghai21_sessions_last_event (last_event_at),
    INDEX idx_shanghai21_sessions_created (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS shanghai21_session_participants (
    id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
    session_id BIGINT UNSIGNED NOT NULL,
    seat_no INT UNSIGNED NOT NULL,
    portal_user_id INT UNSIGNED NULL,
    invited_by_user_id INT UNSIGNED NULL,
    display_name VARCHAR(120) NOT NULL DEFAULT '',
    participant_role ENUM('owner', 'friend', 'guest') NOT NULL DEFAULT 'guest',
    invitation_status ENUM('accepted', 'pending', 'declined', 'removed') NOT NULL DEFAULT 'accepted',
    current_target_index INT UNSIGNED NOT NULL DEFAULT 0,
    current_total_score INT NOT NULL DEFAULT 0,
    is_active TINYINT(1) NOT NULL DEFAULT 1,
    joined_at DATETIME(3) NULL DEFAULT NULL,
    last_throw_at DATETIME(3) NULL DEFAULT NULL,
    created_at DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    updated_at DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
    CONSTRAINT fk_shanghai21_participants_session FOREIGN KEY (session_id) REFERENCES shanghai21_sessions (id) ON DELETE CASCADE,
    CONSTRAINT fk_shanghai21_participants_user FOREIGN KEY (portal_user_id) REFERENCES portal_users (id) ON DELETE SET NULL,
    CONSTRAINT fk_shanghai21_participants_invited_by FOREIGN KEY (invited_by_user_id) REFERENCES portal_users (id) ON DELETE SET NULL,
    CONSTRAINT uq_shanghai21_participants_seat UNIQUE (session_id, seat_no),
    INDEX idx_shanghai21_participants_user_active (portal_user_id, invitation_status, is_active),
    INDEX idx_shanghai21_participants_session_status (session_id, invitation_status, is_active),
    INDEX idx_shanghai21_participants_last_throw (last_throw_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS shanghai21_events (
    id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
    session_id BIGINT UNSIGNED NOT NULL,
    participant_id BIGINT UNSIGNED NULL,
    player_user_id INT UNSIGNED NULL,
    recorded_by_user_id INT UNSIGNED NULL,
    event_no INT UNSIGNED NOT NULL,
    event_type VARCHAR(32) NOT NULL,
    event_source VARCHAR(32) NOT NULL DEFAULT '',
    player_index INT UNSIGNED NULL,
    player_name VARCHAR(120) NOT NULL DEFAULT '',
    target_label VARCHAR(20) NOT NULL DEFAULT '',
    sector_result VARCHAR(32) NOT NULL DEFAULT '',
    score_delta INT NOT NULL DEFAULT 0,
    score_after INT NOT NULL DEFAULT 0,
    detected_at DATETIME(3) NULL DEFAULT NULL,
    payload_json JSON NOT NULL,
    created_at DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    CONSTRAINT fk_shanghai21_events_session FOREIGN KEY (session_id) REFERENCES shanghai21_sessions (id) ON DELETE CASCADE,
    CONSTRAINT fk_shanghai21_events_participant FOREIGN KEY (participant_id) REFERENCES shanghai21_session_participants (id) ON DELETE SET NULL,
    CONSTRAINT fk_shanghai21_events_player_user FOREIGN KEY (player_user_id) REFERENCES portal_users (id) ON DELETE SET NULL,
    CONSTRAINT fk_shanghai21_events_recorded_by FOREIGN KEY (recorded_by_user_id) REFERENCES portal_users (id) ON DELETE SET NULL,
    CONSTRAINT uq_shanghai21_session_event UNIQUE (session_id, event_no),
    INDEX idx_shanghai21_events_session_event (session_id, event_no),
    INDEX idx_shanghai21_events_player_detected (player_user_id, detected_at),
    INDEX idx_shanghai21_events_recorded_by (recorded_by_user_id, created_at),
    INDEX idx_shanghai21_events_detected (detected_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS shanghai42_sessions (
    id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
    owner_user_id INT UNSIGNED NOT NULL,
    participant_summary VARCHAR(1000) NOT NULL DEFAULT '',
    participants_json JSON NOT NULL,
    state_json JSON NOT NULL,
    participant_count SMALLINT UNSIGNED NOT NULL DEFAULT 1,
    event_count INT UNSIGNED NOT NULL DEFAULT 0,
    last_event_at DATETIME(3) NULL DEFAULT NULL,
    is_deleted TINYINT(1) NOT NULL DEFAULT 0,
    created_at DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    updated_at DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
    deleted_at DATETIME(3) NULL DEFAULT NULL,
    CONSTRAINT fk_shanghai42_sessions_owner FOREIGN KEY (owner_user_id) REFERENCES portal_users (id),
    INDEX idx_shanghai42_sessions_owner_active_updated (owner_user_id, is_deleted, updated_at),
    INDEX idx_shanghai42_sessions_last_event (last_event_at),
    INDEX idx_shanghai42_sessions_created (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS shanghai42_session_participants (
    id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
    session_id BIGINT UNSIGNED NOT NULL,
    seat_no INT UNSIGNED NOT NULL,
    portal_user_id INT UNSIGNED NULL,
    invited_by_user_id INT UNSIGNED NULL,
    display_name VARCHAR(120) NOT NULL DEFAULT '',
    participant_role ENUM('owner', 'friend', 'guest') NOT NULL DEFAULT 'guest',
    invitation_status ENUM('accepted', 'pending', 'declined', 'removed') NOT NULL DEFAULT 'accepted',
    current_target_index INT UNSIGNED NOT NULL DEFAULT 0,
    current_total_score INT NOT NULL DEFAULT 0,
    is_active TINYINT(1) NOT NULL DEFAULT 1,
    joined_at DATETIME(3) NULL DEFAULT NULL,
    last_throw_at DATETIME(3) NULL DEFAULT NULL,
    created_at DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    updated_at DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
    CONSTRAINT fk_shanghai42_participants_session FOREIGN KEY (session_id) REFERENCES shanghai42_sessions (id) ON DELETE CASCADE,
    CONSTRAINT fk_shanghai42_participants_user FOREIGN KEY (portal_user_id) REFERENCES portal_users (id) ON DELETE SET NULL,
    CONSTRAINT fk_shanghai42_participants_invited_by FOREIGN KEY (invited_by_user_id) REFERENCES portal_users (id) ON DELETE SET NULL,
    CONSTRAINT uq_shanghai42_participants_seat UNIQUE (session_id, seat_no),
    INDEX idx_shanghai42_participants_user_active (portal_user_id, invitation_status, is_active),
    INDEX idx_shanghai42_participants_session_status (session_id, invitation_status, is_active),
    INDEX idx_shanghai42_participants_last_throw (last_throw_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS shanghai42_events (
    id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
    session_id BIGINT UNSIGNED NOT NULL,
    participant_id BIGINT UNSIGNED NULL,
    player_user_id INT UNSIGNED NULL,
    recorded_by_user_id INT UNSIGNED NULL,
    event_no INT UNSIGNED NOT NULL,
    event_type VARCHAR(32) NOT NULL,
    event_source VARCHAR(32) NOT NULL DEFAULT '',
    player_index INT UNSIGNED NULL,
    player_name VARCHAR(120) NOT NULL DEFAULT '',
    target_label VARCHAR(20) NOT NULL DEFAULT '',
    sector_result VARCHAR(32) NOT NULL DEFAULT '',
    score_delta INT NOT NULL DEFAULT 0,
    score_after INT NOT NULL DEFAULT 0,
    detected_at DATETIME(3) NULL DEFAULT NULL,
    payload_json JSON NOT NULL,
    created_at DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    CONSTRAINT fk_shanghai42_events_session FOREIGN KEY (session_id) REFERENCES shanghai42_sessions (id) ON DELETE CASCADE,
    CONSTRAINT fk_shanghai42_events_participant FOREIGN KEY (participant_id) REFERENCES shanghai42_session_participants (id) ON DELETE SET NULL,
    CONSTRAINT fk_shanghai42_events_player_user FOREIGN KEY (player_user_id) REFERENCES portal_users (id) ON DELETE SET NULL,
    CONSTRAINT fk_shanghai42_events_recorded_by FOREIGN KEY (recorded_by_user_id) REFERENCES portal_users (id) ON DELETE SET NULL,
    CONSTRAINT uq_shanghai42_session_event UNIQUE (session_id, event_no),
    INDEX idx_shanghai42_events_session_event (session_id, event_no),
    INDEX idx_shanghai42_events_player_detected (player_user_id, detected_at),
    INDEX idx_shanghai42_events_recorded_by (recorded_by_user_id, created_at),
    INDEX idx_shanghai42_events_detected (detected_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
